name: project_zaytooon
description: تطبيق زيتون - مع زيتون الجوع يهون
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.0.5
  shared_preferences: ^2.2.2
  cached_network_image: ^3.3.1
  cupertino_icons: ^1.0.6

  # لإضافة دعم الترجمة والتعريب في Flutter
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/zaytooon_logo.png
    # أضف هنا أي صور أو ملفات أخرى تحتاجها في المشروع